<h1>Notes</ht>
<form action="/notes/add" method="POST">
	<input type="text" name="note" placeholder="Write a note" required>
	<button type="submit">Add Note</button>
</form>

<ul>
	<% notes.forEach(note => { %>
		<li><strong><%= note.timestamp %></strong>: <%= note.text %></li>
	<% }) %>
</ul>
